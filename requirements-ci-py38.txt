# Python 3.8 CI profile for the retained legacy import/readiness contract.
#
# This is intentionally narrower than requirements.txt: Python 3.8 validates the
# public legacy entry-point/import surface and the same unit/integration tests,
# while Python 3.9-3.11 CI lanes still install the complete requirements.txt.
# Optional heavyweight ML/provider packages that are not required to import the
# legacy entry point remain validated by the full-runtime lanes instead of being
# redundantly resolved/downloaded on the oldest compatibility interpreter.
requests>=2.31.0
rich>=13.6.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
configparser>=5.3.0
openai>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
redis>=4.6.0
pyjwt>=2.8.0
bcrypt>=4.0.0
ldap3>=2.9.1
flask>=3.0.0
flask-cors>=4.0.0
celery>=5.3.0
docker>=6.1.0
kubernetes>=27.2.0
python-consul>=1.1.0
psutil>=5.9.0
speechrecognition>=3.10.0
pyttsx3>=2.90
pypandoc>=1.11
cvsslib>=1.0.0
websockets>=11.0.0
aiohttp>=3.9.0
jinja2>=3.1.0
